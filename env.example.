# ── Base de datos MySQL (CloudPanel) ─────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_USER=CAMBIA_ESTO
DB_PASSWORD=CAMBIA_ESTO
DB_NAME=CAMBIA_ESTO

# ── Auth (genera un string de 40+ chars aleatorios) ──────
JWT_SECRET=CAMBIA_ESTO_MIN_40_CARACTERES_ALEATORIOS
SUPERADMIN_EMAIL=admin@tu-dominio.com
SUPERADMIN_PASSWORD=CAMBIA_ESTO

# ── Servidor ─────────────────────────────────────────────
NODE_ENV=production
PORT=3000

# ── Groq API (console.groq.com) ──────────────────────────
GROQ_API_KEY=gsk_CAMBIA_ESTO

# ── CORS (tu dominio exacto con https) ───────────────────
CORS_ORIGIN=https://dataella.tech

# ── Rutas de almacenamiento ──────────────────────────────
STORAGE_PATH=/var/www/html/meeting/storage/audio
ATTACH_PATH=/var/www/html/meeting/storage/attachments
